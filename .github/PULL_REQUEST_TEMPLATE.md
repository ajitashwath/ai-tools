## What was wrong

Briefly describe the problem (bug, duplication, dead code, docs gap…).

## What changed

Briefly describe the fix and why it is correct.

## Verification

- [ ] `pytest -q` passes
- [ ] `ruff check aidev tests` and `ruff format --check aidev tests` pass
- [ ] UI: `npm run lint` and `npm run build` pass (if UI touched)
- [ ] `CHANGELOG.md` updated under `[Unreleased]`
